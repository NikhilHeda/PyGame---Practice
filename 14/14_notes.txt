game over functionality

1. Adding game over ise a function called as gameloop and move the entire thing inside this function

2. Add all the variables that will change, inside the function, else they will be treated as constants.

3. 
	gameOver = False
	
	while gameOver:
		gameDisplay.fill(white)
		message_to_screen('message', red)
		pygame.display.update()
		# event handling